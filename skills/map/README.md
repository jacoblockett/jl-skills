# Map

Early prototype of `/map`, a durable, queryable intent graph for agents.

The current milestone proves local embedded SurrealDB state underneath the future skill. The database is authoritative; `/map` and other agents will eventually query it through the CLI rather than reading database files directly.
