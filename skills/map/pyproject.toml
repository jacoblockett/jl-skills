[project]
name = "jl-map-prototype"
version = "0.1.0"
description = "Prototype durable intent graph for the /map skill"
requires-python = ">=3.11"
dependencies = [
  "surrealdb",
]

[project.scripts]
map-state = "map_state:main"

[tool.setuptools]
py-modules = ["map_state"]
